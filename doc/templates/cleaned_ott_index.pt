<html metal:use-macro="load: head.pt">
<div metal:fill-slot="content" tal:define="basename import: os.path.basename">
<p>Propinquity output documentation. The top-level docs are <a href="../index.html">here</a>.
<p>This directory contains the results of pruning OTT version ${cleaned_ott.version} 
so that the root is <a href="https://tree.opentreeoflife.org/taxonomy/browse?id=${cleaned_ott.root_ott_id}">${cleaned_ott.root_ott_id}</a> and the taxa flagged with the 
follwing flags were removed:</p>
<ul>
    <li tal:repeat="flag cleaned_ott.flags_to_prune" tal:content="flag" />
</ul>
<p>This resulted in a taxonomy with a total of ${cleaned_ott.num_nodes} nodes.
${cleaned_ott.num_tips} of these are leaves and
${cleaned_ott.num_non_leaf_nodes} internal nodes 
(${cleaned_ott.num_monotypic_nodes} monotypic and
${cleaned_ott.num_non_leaf_nodes_with_multiple_children} forking internal nodes).
</p>
<p><a href="./cleaned_ott.tre">cleaned_ott.tre</a> is a newick representation of this taxonomy.</p>
<p>The following table groups the OTT Ids pruned by the combination of flag that caused the pruning:</p>
<table>
    <tr><th>Combination of flags</th><th># of nodes sliced off the full tree</th><th>Ott Ids</th></tr>
    <tr tal:repeat="id_eblob cleaned_ott.pruned.items()">
        <td>${id_eblob[0]}</td>
        <td>${len(id_eblob[1]['anc_ott_id_pruned'])}</td>
        <td>${str(id_eblob[1]['anc_ott_id_pruned'])[1:-1]}</td>
    </tr>
</table>
</div>
</html>
