#!/usr/bin/env python
import sys
import os
out_dir = sys.argv[1]
for inp_path in sys.argv[2:]:
    fn = os.path.split(inp_path)[-1]
    assert fn.endswith('.json')
    t = fn[:-5] + '.tre'
    ofn = os.path.join(out_dir, t)
    if (not os.path.exists(ofn)) or (os.path.getmtime(ofn) < os.path.getmtime(inp_path)):
        sys.stdout.write('{}\n'.format(inp_path))
